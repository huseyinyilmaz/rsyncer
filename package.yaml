# This YAML file describes your package. Stack will automatically generate a
# Cabal file when you run `stack build`. See the hpack website for help with
# this file: <https://github.com/sol/hpack>.
name: rsyncer
version: '0.0.0'
github: "githubuser/rsyncer"
license: MIT
author: "Author name here"
maintainer: "Author name here"
# synopsis: A new Haskeleton package.
# description: rsyncer is a new Haskeleton package.
# category: Other

extra-source-files:
- CHANGELOG.md
- LICENSE.md
- package.yaml
- README.md
- stack.yaml

ghc-options: -Wall

library:
  dependencies:
  - base
  source-dirs: library

executables:
  rsyncer:
    source-dirs: executable
    main: Main.hs
    dependencies:
    - base
    - rsyncer
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N

benchmarks:
  rsyncer-benchmarks:
    source-dirs: benchmark
    main: Main.hs
    dependencies:
    - base
    - rsyncer
    - criterion
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N

tests:
  rsyncer-test-suite:
    source-dirs: test-suite
    main: Main.hs
    dependencies:
    - base
    - rsyncer
    - tasty
    - tasty-hspec
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
